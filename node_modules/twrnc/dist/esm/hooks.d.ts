import { TailwindFn, RnColorScheme } from './types';
declare type Options = {
    withDeviceColorScheme: boolean;
};
export declare function useDeviceContext(tw: TailwindFn, opts?: Options): void;
export declare function useAppColorScheme(tw: TailwindFn, initialValue?: RnColorScheme): [
    colorScheme: RnColorScheme,
    toggleColorScheme: () => void,
    setColorScheme: (colorScheme: RnColorScheme) => void
];
export {};
